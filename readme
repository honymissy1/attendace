Todo next
 * Populate database with 6 users...
 * Create a connection to the database to add an event to all  users 
 * Create a connection to db to display all users (later add pagination).
 - we will move to cell leader's page from there


Todo for Cell 
 * 
  